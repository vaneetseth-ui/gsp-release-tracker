# Copy to `.env` for local dev (`.env` is gitignored).
# Production: set `GLIP_WEBHOOK_URL` in your host (e.g. `heroku config:set GLIP_WEBHOOK_URL=...`).
# RingCentral → Glip / team messaging incoming webhook URL.
GLIP_WEBHOOK_URL=
